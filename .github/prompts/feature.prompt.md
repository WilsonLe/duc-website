---
mode: agent
description: Implement a new feature in the portfolio website.
---

# Feature

Implement the requested feature in this Next.js static portfolio website.

## Guidelines

- Follow the existing project structure (App Router, Tailwind CSS, TypeScript).
- Keep the static export constraint in mind (`output: "export"` — no server-side APIs).
- Reuse existing components and utilities wherever possible.
- Write clean, readable TypeScript with proper types (avoid `any`).
- Style with Tailwind CSS utility classes; maintain the existing design language.
- After implementation, confirm `npm run build` passes.
- Provide a brief summary of what was added and which files were changed.

## Context

${input:Describe the feature you want to add. Be as specific as possible.}
